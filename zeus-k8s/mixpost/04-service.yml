---
apiVersion: v1
kind: Service
metadata:
  name: mixpost
  namespace: mixpost
  labels:
    app.kubernetes.io/name: mixpost
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mixpost
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
